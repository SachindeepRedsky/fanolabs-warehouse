// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.nativescript.fanolabs"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
